name:  02 - Trigger Test  

on:
 workflow_dispatch:
 push:
  branches:
      - main
 
jobs:
  trigger-test:
   runs-on: ubuntu-latest 

   steps:
    - name:  Say trigger started 
      run: echo "Workflow  worked

    - name: Checkout repository
      uses: actions/checkout@v4
    
    - name: Show files after checkout 
      run: ls -la

     
